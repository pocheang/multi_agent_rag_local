# Deprecated compatibility notice.
# Canonical deployment configuration: config/env/production.env.example
# Do not put credentials in this file. Set OPENAI_API_KEY in the deployment environment;
# deploy/scripts/config.py generates the remaining local secrets in .runtime/.
# Deploy with: ./deploy/scripts/deploy.sh production balanced
