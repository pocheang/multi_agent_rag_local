# Documentation Assets

Architecture and product images belong in this directory. Add an image only
when it is referenced by a current public document and includes appropriate
alt text at the call site.
