"""Internal shared contracts used by multiple route modules.

Nothing here is registered as a router. These modules expose helper contracts
(currently the standard RAG pipeline execution contract) consumed by admin and
session routes.
"""
